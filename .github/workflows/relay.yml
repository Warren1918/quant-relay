name: A-Share Quant Data Relay Pipeline

on:
  schedule:
    - cron: '*/5 1-7 * * 1-5'
  workflow_dispatch:

jobs:
  update-snapshot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Fetch and Build Snapshot
        run: python fetch_and_build_snapshot.py

      - name: Commit and Push Snapshot
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data/live_snapshot.json
          git commit -m "chore: auto-update market snapshot $(date +'%Y-%m-%d %H:%M:%S')" || exit 0
          git push
